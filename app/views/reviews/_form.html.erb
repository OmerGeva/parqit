<%= simple_form_for([parking_spot, review], remote: true) do |f| %>
  <%= f.input :title %>
  <%= f.input :content %>
  <%= f.input :rating, collection: (1..5).to_a, id:"review_rating", label: "How was the spot?" %>
  <%= f.submit "Leave Review!", class: 'btn btn-primary btn-custom' %>
<% end %>
