<div class="background-color-white">
  <%= cl_image_tag parking_spot.photo, class: 'marker-img' %>
  <h4><%= parking_spot.title %></h4>
  <p><%= parking_spot.address %></p>

  <%= link_to 'See Parking Spot', parking_spot_path(parking_spot), class: 'btn btn-primary btn-sm mb-2'%>
</div>
